/**
 * StoryForge — local API key (not committed to git)
 *
 * 1. Copy this file to:  config.js
 * 2. Set your OpenAI API key: https://platform.openai.com/api-keys
 * 3. Keep config.js private (it is listed in .gitignore).
 *
 * You can use either:
 *   - config.js + python -m http.server   (or any static server), or
 *   - no config.js + OPENAI_API_KEY in .env + python serve.py
 */

window.STORYFORGE_OPENAI_KEY = 'PASTE_YOUR_OPENAI_API_KEY_HERE';

/** Optional: some setups use this name instead; story.js reads it too. */
// var API_KEY = 'PASTE_YOUR_OPENAI_API_KEY_HERE';
